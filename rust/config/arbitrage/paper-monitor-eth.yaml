# Strict allowlist and freshness profile consumed by arbitrage paper one-shot.
exchanges:
  - paper-left
  - paper-right
symbols:
  - ETH-USDC-PERP
health_check:
  data_timeout: 30
  max_pair_skew_ms: 1000
