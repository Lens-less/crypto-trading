[package]
name = "crypto-trading-config"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Bounded, fail-closed loaders for the trading system's YAML and JSON configuration surface."
# The workspace ships as an application, not as crates.io libraries.
# Publishing is refused explicitly so it cannot happen by accident.
publish = false

[dependencies]
crypto-trading-domain = { path = "../domain" }
rust_decimal.workspace = true
serde.workspace = true
serde_yaml.workspace = true
thiserror.workspace = true

[dev-dependencies]

[lints]
workspace = true
